[workspace]
resolver = "2"

members = ["broodrep", "broodrep-cli", "broodrep-wasm"]


# Enable more optimizations for dependencies in dev, but not for our code
[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = true
